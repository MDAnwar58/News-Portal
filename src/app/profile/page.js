import React from "react";
import ProfileContent from "./ProfileContent";

export const metadata = {
   title: "News Portal - Profile",
};
export default function Profile() {
   return <ProfileContent />;
}
